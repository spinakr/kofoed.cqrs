version: 2.1

jobs:
  build:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.0.100-preview6-disco
    steps: 
      - checkout
      - run:
          name: Restore packages
          command: 
            dotnet restore
      - run :
          name: Build projects
          command: 
            dotnet build
